<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./particles/head'); %>
  </head>
  <body>
    <div class="container">
      <div class="row mt-3">
        <div class="col d-flex justify-content-center">
          <h1 class="text-white">Top Artists:</h1>
        </div>
      </div>
      <% topArtists.forEach((a, i) => { %>
      <div class="row my-2">
        <div
          class="
            col-sm-10
            offset-sm-1
            col-lg-6
            offset-lg-3
            d-flex
            flex-row
            justify-content-between
          "
        >
          <div class="d-flex flex-row">
            <img src="<%= a.image %>" alt="cover" class="mini_cover" />
            <div class="ms-2 align-self-center">
              <a
                href="<%= a.uri %>"
                class="recently-text text-truncate text-white"
                style="max-width: 250px"
                ><%= a.name %></a
              >
            </div>
          </div>
          <div class="align-self-center h3 top-number"><%= i + 1 %></div>
        </div>
      </div>
      <% }); %>
      <div class="row">
        <div class="col-10 offset-1 col-lg-4 offset-lg-4">
          <hr />
        </div>
      </div>
      <div class="row my-3">
        <div
          class="
            col-sm-10
            offset-sm-1
            col-lg-6
            offset-lg-3
            d-flex
            flex-row
            justify-content-center
          "
        >
          <a href="/">Back</a>
        </div>
      </div>
    </div>
  </body>
</html>
